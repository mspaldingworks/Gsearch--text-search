import csv
import re
# Import the email modules we'll need
from email.message import EmailMessage

File_object = open(r"Gsearchdata.txt","r")

# pull dates from each email and put into dates dictionary
requests.get(date_to_search)

    date_to_search = (''\d\d.\d\d.\d\d'')

contents = file. read()
dictionary = ast. literal_eval(contents)
file. close()
print(type(dictionary))
print(dictionary)

pattern = re.compiler(r'\d')

matches = pattern.finditer(date_to_search)

if matches = True
    put()

for match in matches:
    print(match)

# pull emails of collaborators from doc as a dictionary 


# pull keywords from doc on lines starting with the phrase "keyword", and ending after the string of 

# put response in dataframe